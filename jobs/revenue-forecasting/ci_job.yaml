build-job-revenue-forecasting:
  docker:
    - image: << pipeline.parameters.git-image >>
  steps:
    - checkout
    - compare-branch:
        pattern: ^jobs/revenue-forecasting/
    - setup_remote_docker:
        version: << pipeline.parameters.docker-version >>
    - run:
        name: Build Docker image
        command: docker build -t app:build jobs/revenue-forecasting/
    - run:
        name: Test Code
        command: docker run app:build pytest --flake8 --black