build-job-release-scraping:
  docker:
    - image: << pipeline.parameters.git-image >>
  steps:
    - checkout
    - compare-branch:
        pattern: ^jobs/release_scraping/
    - setup_remote_docker:
        version: << pipeline.parameters.docker-version >>
    - run:
        name: Build Docker image
        command: docker build -t app:build jobs/release_scraping/
    - run:
        name: Test Code
        command: docker run app:build pytest --flake8 --black
