build-job-telemetry-dev-cycle:
  docker:
    - image: << pipeline.parameters.git-image >>
  steps:
    - checkout
    - compare-branch:
        pattern: ^jobs/telemetry-dev-cycle/
    - setup_remote_docker:
        version: << pipeline.parameters.docker-version >>
    - run:
        name: Build Docker image
        command: docker build -t app:build jobs/telemetry-dev-cycle/
    - run:
        name: Test Code
        command: docker run app:build pytest --flake8 --black