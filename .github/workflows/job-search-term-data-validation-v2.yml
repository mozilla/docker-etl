###
# This file was generated by docker-etl/ci_config.py.
# Changes should be made to job ci_job.yaml files and re-generated.
###

name: search-term-data-validation-v2

on:
  push:
    branches:
      - main
    paths:
      - 'jobs/search-term-data-validation-v2/**'
      - '.github/workflows/job-search-term-data-validation-v2.yml'
  pull_request:
    paths:
      - 'jobs/search-term-data-validation-v2/**'
      - '.github/workflows/job-search-term-data-validation-v2.yml'

jobs:
  build-job-search-term-data-validation-v2:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Build the Docker image
        # yamllint disable
        run: |
          docker build jobs/search-term-data-validation-v2 -t us-docker.pkg.dev/moz-fx-data-artifacts-prod/docker-etl/search-term-data-validation-v2:latest
        # yamllint enable
      - name: Test Code
        run: docker run us-docker.pkg.dev/moz-fx-data-artifacts-prod/docker-etl/search-term-data-validation-v2:latest pytest
      - name: Save Docker image
        run: |
          docker save us-docker.pkg.dev/moz-fx-data-artifacts-prod/docker-etl/search-term-data-validation-v2:latest | gzip > /tmp/search-term-data-validation-v2-image.tar.gz
      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: search-term-data-validation-v2-docker-image
          path: /tmp/search-term-data-validation-v2-image.tar.gz
          retention-days: 1

  deploy-to-gar-search-term-data-validation-v2:
    name: Deploy search-term-data-validation-v2 to GAR
    runs-on: ubuntu-latest
    needs: [build-job-search-term-data-validation-v2]
    if: github.ref == 'refs/heads/main'
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Download Docker image artifact
        uses: actions/download-artifact@v4
        with:
          name: search-term-data-validation-v2-docker-image
          path: /tmp
      - name: Load Docker image
        run: |
          docker load < /tmp/search-term-data-validation-v2-image.tar.gz
      - name: Push Docker image latest to GAR
        uses: mozilla-it/deploy-actions/docker-push@v4.3.2
        with:
          project_id: moz-fx-data-artifacts-prod
          image_tags: us-docker.pkg.dev/moz-fx-data-artifacts-prod/docker-etl/search-term-data-validation-v2:latest
          workload_identity_pool_project_number: ${{ vars.GCPV2_WORKLOAD_IDENTITY_POOL_PROJECT_NUMBER }}
          service_account_name: docker-etl
