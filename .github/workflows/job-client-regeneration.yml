###
# This file was generated by docker-etl/ci_config.py.
# Changes should be made to job ci_job.yaml files and re-generated.
###

name: client-regeneration

on:
  push:
    branches:
      - main
    paths:
      - 'jobs/client-regeneration/**'
      - '.github/workflows/job-client-regeneration.yml'
  pull_request:
    paths:
      - 'jobs/client-regeneration/**'
      - '.github/workflows/job-client-regeneration.yml'

jobs:
  build-job-client-regeneration:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Build the Docker image
        # yamllint disable
        run: |
          docker build jobs/client-regeneration -t us-docker.pkg.dev/moz-fx-data-artifacts-prod/docker-etl/client-regeneration:latest
        # yamllint enable
      - name: Test Code
        run: docker run us-docker.pkg.dev/moz-fx-data-artifacts-prod/docker-etl/client-regeneration:latest pytest --flake8 --black
      - name: Save Docker image
        run: |
          docker save us-docker.pkg.dev/moz-fx-data-artifacts-prod/docker-etl/client-regeneration:latest | gzip > /tmp/client-regeneration-image.tar.gz
      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: client-regeneration-docker-image
          path: /tmp/client-regeneration-image.tar.gz
          retention-days: 1

  deploy-to-gar-client-regeneration:
    name: Deploy client-regeneration to GAR
    runs-on: ubuntu-latest
    needs: [build-job-client-regeneration]
    if: github.ref == 'refs/heads/main'
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Download Docker image artifact
        uses: actions/download-artifact@v4
        with:
          name: client-regeneration-docker-image
          path: /tmp
      - name: Load Docker image
        run: |
          docker load < /tmp/client-regeneration-image.tar.gz
      - name: Push Docker image latest to GAR
        uses: mozilla-it/deploy-actions/docker-push@v4.3.2
        with:
          project_id: moz-fx-data-artifacts-prod
          image_tags: us-docker.pkg.dev/moz-fx-data-artifacts-prod/docker-etl/client-regeneration:latest
          workload_identity_pool_project_number: ${{ vars.GCPV2_WORKLOAD_IDENTITY_POOL_PROJECT_NUMBER }}
          service_account_name: docker-etl
