###
# This file was generated by docker-etl/ci_config.py.
# Changes should be made to job ci_job.yaml files and re-generated.
###

name: mozaggregator2bq

on:
  push:
    branches:
      - '**'
    paths:
      - 'jobs/mozaggregator2bq/**'
      - '.github/workflows/job-mozaggregator2bq.yml'
  pull_request:
    paths:
      - 'jobs/mozaggregator2bq/**'
      - '.github/workflows/job-mozaggregator2bq.yml'

jobs:
  build-job-mozaggregator2bq:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Build the Docker image
        # yamllint disable
        run: |
          docker build jobs/mozaggregator2bq -t us-docker.pkg.dev/moz-fx-data-artifacts-prod/docker-etl/mozaggregator2bq:latest
        # yamllint enable

  deploy-to-gar-mozaggregator2bq:
    name: Deploy mozaggregator2bq to GAR
    runs-on: ubuntu-latest
    needs: [build-job-mozaggregator2bq]
    if: github.ref == 'refs/heads/main'
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Build the Docker image
        # yamllint disable
        run: |
          docker build jobs/mozaggregator2bq -t us-docker.pkg.dev/moz-fx-data-artifacts-prod/docker-etl/mozaggregator2bq:latest
        # yamllint enable
      - name: Push Docker image latest to GAR
        uses: mozilla-it/deploy-actions/docker-push@v4.3.2
        with:
          project_id: moz-fx-data-artifacts-prod
          image_tags: us-docker.pkg.dev/moz-fx-data-artifacts-prod/docker-etl/mozaggregator2bq:latest
          workload_identity_pool_project_number: ${{ vars.GCPV2_WORKLOAD_IDENTITY_POOL_PROJECT_NUMBER }}
          service_account_name: docker-etl
