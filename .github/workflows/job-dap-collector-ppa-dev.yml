###
# This file was generated by docker-etl/ci_config.py.
# Changes should be made to job ci_job.yaml files and re-generated.
###

name: dap-collector-ppa-dev

on:
  push:
    branches:
      - main
    paths:
      - 'jobs/dap-collector-ppa-dev/**'
      - '.github/workflows/job-dap-collector-ppa-dev.yml'
  pull_request:
    paths:
      - 'jobs/dap-collector-ppa-dev/**'
      - '.github/workflows/job-dap-collector-ppa-dev.yml'

jobs:
  build-job-dap-collector-ppa-dev:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Build the Docker image
        # yamllint disable
        run: |
          docker build jobs/dap-collector-ppa-dev -t us-docker.pkg.dev/moz-fx-data-artifacts-prod/docker-etl/dap-collector-ppa-dev:latest
        # yamllint enable
      - name: Test Code
        run: docker run us-docker.pkg.dev/moz-fx-data-artifacts-prod/docker-etl/dap-collector-ppa-dev:latest python3 -m pytest
      - name: Save Docker image
        run: |
          docker save us-docker.pkg.dev/moz-fx-data-artifacts-prod/docker-etl/dap-collector-ppa-dev:latest | gzip > /tmp/dap-collector-ppa-dev-image.tar.gz
      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: dap-collector-ppa-dev-docker-image
          path: /tmp/dap-collector-ppa-dev-image.tar.gz
          retention-days: 1

  deploy-to-gar-dap-collector-ppa-dev:
    name: Deploy dap-collector-ppa-dev to GAR
    runs-on: ubuntu-latest
    needs: [build-job-dap-collector-ppa-dev]
    if: github.ref == 'refs/heads/main'
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Download Docker image artifact
        uses: actions/download-artifact@v4
        with:
          name: dap-collector-ppa-dev-docker-image
          path: /tmp
      - name: Load Docker image
        run: |
          docker load < /tmp/dap-collector-ppa-dev-image.tar.gz
      - name: Push Docker image latest to GAR
        uses: mozilla-it/deploy-actions/docker-push@v4.3.2
        with:
          project_id: moz-fx-data-artifacts-prod
          image_tags: us-docker.pkg.dev/moz-fx-data-artifacts-prod/docker-etl/dap-collector-ppa-dev:latest
          workload_identity_pool_project_number: ${{ vars.GCPV2_WORKLOAD_IDENTITY_POOL_PROJECT_NUMBER }}
          service_account_name: docker-etl
